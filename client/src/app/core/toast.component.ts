import { ChangeDetectionStrategy, Component, inject } from '@angular/core';
import { ToastService } from './toast.service';
@Component({ selector: 'app-toasts', template: `<div class="toasts" aria-live="polite">@for (toast of service.messages(); track toast.id) {<button type="button" [class]="toast.kind" (click)="service.dismiss(toast.id)">{{ toast.message }}<span>×</span></button>}</div>`, styles: `.toasts{position:fixed;z-index:100;right:1rem;bottom:1rem;display:grid;gap:.6rem;width:min(380px,calc(100vw - 2rem))}.toasts button{display:flex;justify-content:space-between;gap:1rem;padding:.85rem 1rem;border:1px solid var(--line);border-left:4px solid var(--brand);border-radius:.65rem;background:var(--surface);box-shadow:var(--shadow);color:var(--ink-950);text-align:left;cursor:pointer}.toasts .error{border-left-color:var(--red)}.toasts .success{border-left-color:#2d936c}`, changeDetection: ChangeDetectionStrategy.OnPush })
export class ToastComponent { readonly service = inject(ToastService); }
